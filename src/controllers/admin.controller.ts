import { NextFunction } from 'express';

// Get all users
export const getAllUsers = async (next: NextFunction) => {
  try {
  } catch (error) {
    next(error);
  }
};

// Assign role to user
export const assignRole = async (next: NextFunction) => {
  try {
  } catch (error) {
    next(error);
  }
};

// Remove role from user
export const removeRole = async (next: NextFunction) => {
  try {
  } catch (error) {
    next(error);
  }
};

// Delete gym
export const deleteGym = async (next: NextFunction) => {
  try {
  } catch (error) {
    next(error);
  }
};

// Get dashboard statistics
export const getDashboardStats = async (next: NextFunction) => {
  try {
  } catch (error) {
    next(error);
  }
};
